# NOVA RIFT — Frontend (Expo). Copia este archivo a .env si necesitas overrides.
# NORMALMENTE NO HACE FALTA: apiConfig.js autodetecta el host del backend
# (web usa el hostname del navegador; emulador Android usa 10.0.2.2).

# Solo para probar en MOVIL FISICO por WiFi: IP de LAN del PC con el backend.
# EXPO_PUBLIC_API_HOST=192.168.1.38
